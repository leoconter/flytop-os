import { ScreenPlaceholder } from "@/components/app-shell/screen-placeholder";

export const metadata = { title: "FlyTop OS · Tela do Vendedor" };

export default function VendedorPage() {
  return (
    <ScreenPlaceholder
      eyebrow="Sua performance"
      title="Tela do Vendedor"
      sub="Metas, ranking de equipes e vendas recentes"
    />
  );
}
