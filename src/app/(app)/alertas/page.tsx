import { ScreenPlaceholder } from "@/components/app-shell/screen-placeholder";

export const metadata = { title: "FlyTop OS · Alertas" };

export default function AlertasPage() {
  return (
    <ScreenPlaceholder
      eyebrow="Comunidade · operação"
      title="Controle de Alertas"
      sub="Cadastre, pré-visualize e dispare ofertas nos grupos"
    />
  );
}
