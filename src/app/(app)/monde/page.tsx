import { ScreenPlaceholder } from "@/components/app-shell/screen-placeholder";

export const metadata = { title: "FlyTop OS · Monde & Sincronização" };

export default function MondePage() {
  return (
    <ScreenPlaceholder
      eyebrow="Integração"
      title="Monde & Sincronização"
      sub="Origem dos dados de venda e clientes"
    />
  );
}
